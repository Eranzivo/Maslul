# Maslul — Backlog & History

> **North star + reprioritized roadmap (2026-06-13):** `outputs/product-vision-roadmap_2026-06-13.md`
> — *Maslul is an AI dispatch cockpit, not a calendar.* Buckets below mirror it.

## 🟢 NOW — coordinator flow redesign (Israel demo feedback 2026-06-14)
> Full triage: `outputs/israel-feedback-triage_2026-06-14.md`. Theme: dead-simple dispatcher flow, optimization behind the scenes.

> **⏸ PAUSED (2026-06-15) — PureWater zone/scheduling deep-dive** ("still seems off, get back to it"). Full state + resume plan: `outputs/zone-gap-analysis_2026-06-15.md`. TL;DR: far→near backtrack FIXED in engine (`995bc28`, **pushed to origin/main; Railway live v1.2.0** — see audit note); 20 pending = capacity (תל אביב 27/לוד-אשדוד 25/דרום 19 vs 2 covering days × max_daily 9) + חרב(no coords) + סלעית(wrong zone). **✅ orphan zones cleaned 14→9 (2026-06-27)** — see `outputs/zone-merge-plan_2026-06-27.md`. Resume: fix סלעית → geocode the ~10 no-coord settlements → Israel decides 3rd-covering-day vs spill → confirm fix is live (dry-run) → dry-run re-batch.
> **↳ audit 2026-06-24:** `995bc28` + `79dd2fb` are on `origin/main`; Railway `/health` = ok, v1.2.0. The "committed not deployed" wording was a mid-session snapshot — the deploy has since landed. Next session: confirm the far→near build is the running one via a dry-run (expect נהריה→…→חיפה, no revisit) before trusting the re-batch. **Still pending (DB, not code): set PureWater `config.defaults.work_days=[0,1,2,3,4]`** (today Fri-off relies on per-tech `weekly_schedule` only). _(✅ done 2026-06-29.)_
> **↳ 2026-06-30 (live review):** **zones rebuilt 9→8** from Israel's real 20-month export (405 cities, MD5-verified, `geo_places` untouched) — see `context/clients/purewater.md`. Built a demand dashboard from the same export (`outputs/purewater-review_2026-06-29/dashboard_20mo_2026-06-30.html`). **✅ RESOLVED 2026-06-30:** Israel clarified → יקנעם-נתניה = שרון (no 9th zone; stays 8); מודיעין → ירושלים; אשקלון → both דרום + שפלה (depot dual-membership). **Rotation wired + verified** for all 3 techs; scheduling operational again. **Remaining:** geocode the ~250 coord-less cities → aliases (יקנעם→יוקנעם, קיבוץ שובל→שובל) → order each zone far→near → decide מרכז לוד-אשדוד 3rd covering day (2 given, ~2.6 needed) → optional rename zones to Israel's rotation labels → 7 old-task settlements (add or ignore).

> **✅ UI redesign port SHIPPED & DEPLOYED 2026-06-15** (7 slices on `md-*` design system, pushed to live):
> detail side-panel, weekly 3h window-blocks, daily grid polish, coordinator **3-card chooser** (additive over `showCandidate`),
> home dashboard, nav sidebar. All handlers preserved, 61+18 tests green. Source: `mockups/claude-design/`, log: `mockups/DESIGN-LOG.md`.
- [ ] **⭐ Manual E2E/QA pass — every section** (Eran request 2026-06-15; small fixes expected, nothing broken so far). Click through + verify each: home (KPI + tech→weekly), dispatch/coordinator (search→3 cards→reveal→confirm→back-home), task detail (assign/transfer, lock, status, save), weekly board (window-blocks, drag, optimize), daily grid (drop geometry, now-line, tray), nav + role/tenant switch, calls tab, reports, technicians, zones, clients, settings. Log each fix as found.
- [ ] **Simplified scheduling flow (Feedback #3 + 1.4/1.6/1.7)** — Search (city/address) → **3 recommendation cards** (Day · Date · Time-window only, no tech/route/scores) → confirm. Reveal tech/route/existing-day-jobs only *after* a card is picked. Replaces the current multi-slot `showCandidate`. **(card chooser shipped 2026-06-15; remaining: "check specific date" calendar view, auto-return-home polish.)**
  - [ ] **"Find Another Date"** → next-best optimized option (then 3rd…), all still rule-compliant.
  - [ ] **"Check Specific Date"** → run the same engine against a coordinator-chosen date; show that day's open windows calendar-style (#1.7/2.5).
  - [ ] After confirm: **"נקבע ✓" → auto-return to home/search** (#1.9/2.9).
- [ ] **Home = technician names only (#1.10)** — click a tech → structured weekly view (days · windows · cities · addresses · service types · status). Replaces current task/city dashboard.
- [ ] **Chronological ordering audit (#1.8/2.8)** — never 10:00 before 07:00; weekly sort fixed, audit all views. Calendar shows **only confirmed/completed**, never drafts/candidates.
- [ ] **Pass 1 — design system + shell / side-panel redesign.** Tokens, Linear-style side panel, cockpit dashboard, reusable components. (In progress.)
- [ ] **Explainability v1** — "why this tech" panel from signals the engine already computes (same zone · km saved · uses active day · avoids new route · load impact) + hover tooltips. (Now deferred behind the card flow — shown *after* selection per #3.)

## 🟠 Next
### Scheduling-engine capabilities (Israel feedback 2026-06-14)
- [ ] **Fill-first over balance (#1.5/2.3/2.7)** — Israel wants consolidation (pack one tech's day to max before opening another), NOT even-spread. Flip PureWater `scheduling.balance.enabled` → OFF. Re-validate `_assignment_score` fill-first path. ⚠ reverses the 2026-06-13 balance-ON decision.
- [x] **⭐ Bulk-import → batch engine ("fill a week at once")** ✅ 2026-06-15 (`5acf4cd`) — `runBulkImport()` now offers **⚡ שבץ אוטומטית** → dry-run PREVIEW → commit, calling `/batch-schedule`. Backend accepts the coordinator's own Supabase user-JWT (introspected via GoTrue, tenant FORCED to caller's; techs denied; super_admin may impersonate) OR the service key. Pure authz in `backend/batch_auth.py` + 8 tests; CORS allows Authorization. Generic per tenant. **CSV-file import path not yet wired to batch (low priority — Israel won't use file import).**
- [x] **Configurable working days per tenant/tech (#2026-06-15)** ✅ 2026-06-15 — `config.defaults.work_days` (array of weekday ints; absent ⇒ Sat-off default, back-compat). Pure helpers `isTenantWorkDay`(JS)/`tenant_works_day`(Py), honored by BOTH live (`getNextDates`/`isTechAvailable`) + batch (`tech_is_working`), AND-combined with per-tech `weekly_schedule.work`. Settings "ימי עבודה" toggle row added. TDD: 10 JS checks + 3 Py tests. ⏳ set PureWater `work_days=[0,1,2,3,4]` (currently relies on per-tech Fri-off); batch part deploys with `995bc28`. Original ask below:
- [ ] ~~**Configurable working days per tenant/tech (#2026-06-15)**~~ — set each tenant's operating week so calls NEVER land on off-days. PureWater = Sun–Thu (Fri+Sat off, always). Today Saturday is hardcoded-excluded in `batch_schedule.py` and Friday is only avoided implicitly (empty rotation slot) — too fragile. Make it explicit, honoured by BOTH the batch engine and the live `_candidatesZone`/`getNextDates` path. **Placement (Eran 2026-06-15): this belongs in the Settings page (tenant default) and/or at the technician level (`technicians.weekly_schedule[d].work` already exists per-tech) — it is NOT a standalone feature like vacations/חופשות.** A simple "ימי עבודה" toggle row in settings + per-tech override. Generic (each client sets their own week); absent config = today's behaviour.
- [x] **Strict daily-region HARD block in dispatch UI (#1.3/1.11)** ✅ 2026-06-24 — `_candidatesZone` (dispatch search) already hard-excluded; the gap was the 3 manual placement paths (`_onCellDrop`/`_onGridDrop`/`placeTaskDetail`) which only soft-warned via `confirmZoneDrop`, ignoring `zone_strict`. New pure `zoneDropDecision(sc,hasMismatch)` → block/warn/allow: **`zone_strict` (default true) ⇒ hard block (error toast, no override)**; relaxed ⇒ soft `zone_drop_guard` warn; strict dominates the guard. TDD: 8 checks in `tests/zones.test.js` (35 green). Doc: `context/scheduling-rules.md`. **Remaining sub-item: remove the N/S boundary heuristic (#1.2/2.11)** — folds into "real boundary engine" below.
- [ ] **Technician skills / categories (#2.1)** — required `technicians.skills[]`; candidate engine filters techs by skill. Not every tech does every job type.
- [ ] **Per-category daily limits (#2.4)** — `technicians.category_limits` (e.g. ≤2 service calls, ≤5 installs/day); assigner respects them.
- [ ] **Rotation variety + busy-zone coverage (#2.2)** — up to 5 weekly regions/tech, ensure a tech isn't stuck in one area all week; **give under-covered busy zones a 3rd covering day** (תל אביב = 27 calls vs 2 covering tech-days is the current overflow cause).
- [ ] **Mandatory tech config + real boundary engine (#2.10/2.11)** — can't create a tech without regions, skills, hours, durations, max-daily, categories; recommendations never exceed the defined operating range (region/coords based, not N/S).
- [ ] **Bulk region creation (#2.12)** — paste a list of ~100 cities into a region at once (zone authoring; pairs with `canonicalCity` guard).
- [ ] **Polygon parity with timing.tech territories (Eran 2026-07-05, screenshots)** — multiple adjustable polygons per zone incl. sub-city splits (their UI shows ~10 rings around one city, km² labels, vertex editing). ✅ shipped so far: brain-backed detection, WYSIWYG multi-ring editor (vertex-edit + per-ring delete via toolbar), **circle tool → editable 36-vertex ring (2026-07-06)**, **national OSM import → geo_places = 1,310 places incl. all small settlements (2026-07-06; closed Eran's live-QA misses תפרח/משמר הנגב/גבעות בר)**. Remaining: km² labels, and (their reference bar) AI-adjusted territories by season/day/density. Tech GPS-on-map exists already (coordinator live map) — not mandatory now.
- [ ] **Geo curation follow-ups from the OSM import (2026-07-06)** — (1) normalizer collision: ג'ת (ואדי עארה) won key `גת`; kibbutz גת + ג'ת הגלילית dropped — needs curated aliases/qualified keys; ~10 same-key OSM dups logged in `outputs/` report; (2) add `דיר אל אסד` as alias of existing `דייר אל אסד` (skipped as near-dup row); (3) WB/Golan coverage came via the OSM IL boundary — spot-check if a client needs יו"ש settlements beyond what's already seeded. **(4) Eran's decision 2026-07-06: gov-list cross-validation NOT needed now** — brain coverage is visually complete (CBS registry ~79% + 100% of PureWater-relevant); coordinators will be instructed on correct city spelling and manual corrections handle the rest. **Ready-made inputs for the future pass (produced zero-write by the parallel "purewater pilot live feedback" session):** `israeli-settlements-master_2026-07-06.csv` (all 1,306 CBS settlements + codes), `settlements-gap-missing_2026-07-06.csv` (276 official names not in brain), `brain-nonofficial-keys_2026-07-06.csv` (308 brain keys not matching CBS spelling). NOTE: the 276 + 308 heavily overlap — same places under CBS-vs-OSM spellings (אבו קרינאת vs אבו קורינאת) — so the pass is mostly ALIAS reconciliation to CBS-canonical names, not new places. Column I = שם מועצה (regional council) could later drive council-based zone authoring.
- [ ] **45-min "package" category (#1.1)** — add the DB category; durations already flow through the engine (standard 30 / package 45).
- [x] **Out-of-zone drop safeguard** ✅ 2026-06-13 — `confirmZoneDrop`/`zoneDropMismatch` on all three placement paths warn (fail-soft) when a call's city is in a different zone than the tech's day zone; ⚠ flag via `taskOutOfZone`. Config `scheduling.zone_drop_guard` (default ON, zone-mode). Needs browser QA. **→ upgrade to hard-block per #1.3.**
- [ ] **Calculate/batch-schedule PureWater's 108 tasks** — DEFERRED until all scheduling changes land (mode-aware UI + Plan B). Prereqs: (1) re-run rotation SQL so tech rotations re-link to current zone IDs; (2) verify Israel's tech-zone-per-day division (confirmed 2026-06-10 = `migration-purewater-zones-rotation_2026-06-05.sql`); (3) city aliases קש→קריית שמונה, זכרון→זכרון יעקב (added 2026-06-10, JS + backend). Then run batch assignment respecting the rotation.
- [ ] Israel fills in client details on 108 tasks (via ✏️ edit button)
- [ ] Israel testing — real dispatch scenarios, feedback collection
- [ ] **⚠ Unify placement policy (found 2026-06-29 review)** — `scheduling.balance.enabled` behaves **oppositely** across engines (live `balanceAdjust` consolidates vs. batch `_assignment_score` spreads), and `scheduling.equal_city_distribution` is **half-wired** (live `_candidatesZone` only; batch ignores it, and even live it's swamped by the +100 fill-first weight). Two soft biases + fill_first stack additively on one score and partly cancel → unpredictable placement + re-batch churn. **Fix:** collapse to ONE `placement_policy` (`consolidate`/`spread`) + `same_city` (`group`/`split`) read identically by dispatch AND batch; delete the divergent paths; add tests asserting both engines agree. Decide policy with Israel first. Detail: `outputs/purewater-review_2026-06-29/meeting-packet.md` Sec. 5B.
- [ ] ~~Equal city distribution — config flag `scheduling.equal_city_distribution` to spread same-city tasks across techs~~ → folded into the placement-policy unification above (flag exists but doesn't truly enforce).
- [ ] Admin panel chips redesign — plan at `.claude/plans/ancient-plotting-prism.md`
- [ ] Web Push notifications — alert tech when task assigned

## 🟡 After Israel stabilizes
- [ ] **Traffic-aware routing — infra prep (Eran 2026-07-06)** — Waze-style live traffic per leg. Facts: Waze has NO official routing API (unofficial libs are ToS-risky); the real tool is Google DM **traffic-aware SKU** (`departure_time` → `duration_in_traffic`, ~2× element price) or Routes API. Design (cheap now, pay later): (1) add `time_bucket` column to `route_cache` (default `'static'` — today's rows unchanged); (2) new knob `routing.traffic_mode: off | rush_hour | live` (PureWater = `off` — Negev inter-city, traffic-light); `rush_hour` = static duration × configurable per-tenant rush multiplier windows (zero API cost); `live` = re-solve tomorrow's routes each evening with `departure_time` (small element count ≈ tomorrow's legs only, NOT the whole matrix — keeps quota tiny); (3) both engines read the knob (registry row + test); (4) **learned durations (Eran 2026-07-06):** log OBSERVED leg durations from real operations (assignment/en_route/arrived timestamps we already store) into time-bucketed `route_cache` rows — the brain learns "יהוד→ת"א = 60min @09:00 / 30min @11:00" from its own history, zero API cost, accuracy compounds per client (fits [geo-foundation-vision]: global travel-patterns layer). The 3h customer window absorbs estimate error meanwhile — that's partly WHY Israel uses 3h windows (drive-time slack + room for added calls + one-area-per-day efficiency). Gush-Dan client #2 is the trigger for `live`. Handover §17 keeps real-time prediction a non-goal for the pilot.
- [ ] **⭐ Per-task scheduling constraints — NEXT ENGINE SLICE #1** (Israel's handover §8/§10/§13 + Eran 2026-07-06) — structured `earliest`/`latest`/`forbidden_times`, `fixed_date`, `priority`, `requires_approval`, `contact_person`; optimizer must honor them (solver window infra already exists — `window_start/end` are hard constraints). Today buried in free-text `notes`. **Eran 2026-07-06 addition: customer "preferred time windows" must support DAYS, not just hours** — e.g. "only Sundays/Tuesdays, 10:00–13:00" — so the engine directs the call to the right tech+day per the tenant's business logic (rotation, zones, skills). Intake fields + both-door readers + knob-registry rows + solver mapping, one slice.
- [ ] **"Call N min before arrival"** per-task notification rule (WhatsApp).
- [ ] **Variable window length per task** (not hardcoded 3h — saw 1.5h/3h/4h in real data).
- [ ] **Basic CRM structured fields** (the next-client direction; already needed by Israel): `product`/model, `price`/quote, `job_type` (לקחת/לספק/להתקין/לתקן), `contact`. Schema accurate + connected end-to-end. See `context/clients/purewater.md` → "Signals from Israel's real calendar".
- [ ] Tech view redesign
- [ ] **Geo corrections loop / self-healing place brain** — additive, does NOT touch routing. Phases: (1) write `place_resolution_log` + `confidence` on `geo_places`; (2) super_admin Geo Health page (see→fix→save→re-assign→log, reversible); (3) on-demand digest output; (4) typeahead-constrained city input at all entry doors; (5) bulk-door fuzzy+centroid ladder; (6) technician-GPS ground-truth healing. Two-store confidence model (high bar to enter the brain, below-bar surfaces as work). Fail-soft, never blocks. Full design: `outputs/geo-corrections-loop-design_2026-06-13.md`.
- [ ] **UI/UX design pass** — clean friendly SaaS look (colour-coded calendar + side panel, KPI strip), operable by a non-technical user. Refs: `outputs/ui-references_2026-06-13.md`. After the editable-calendar engine slices.
- [ ] **Dashboard & analytics — charts, KPIs (like timing.tech)** — incl. an **in-app demand dashboard** (live version of the static report built 2026-06-30 for PureWater): demand-by-zone · **demand-vs-coverage with under/over-covered flags** · top-cities · long-tail/concentration — refreshing from real tenant data so Israel can open it himself. `reports_enabled` flag already ON. Prototype + spec to port: `outputs/purewater-review_2026-06-29/dashboard_20mo_2026-06-30.html`. **Recurring deliverable angle:** re-run demand-vs-coverage on every new client export to catch seasonal shifts before they become overflow (see [zone-demand-coverage-habit]). _Added 2026-06-30 at Israel's review; awaiting his go on scope._
- [ ] Customer ETA portal — SMS/WhatsApp link → customer sees tech ETA + can rate

## 🟢 Foundations / infra ("on me")
- [ ] **Workspace cleanup — merge/remove/reorganize outputs/ + stale files (Eran 2026-07-06; assign to Opus 4.8 when Fable tokens run out).** outputs/ has ~40+ dated artifacts (old plans, superseded migrations, review folders); archive superseded ones into outputs/archive/, dedupe overlapping design docs, verify nothing referenced by context/ breaks. Rule: never delete migrations that were actually applied; keep requirement sources (israel-handover, reviews). context/ itself was reorganized 2026-07-06 (README.md map) — cleanup is mostly outputs/ + root strays (PDF, handoff prompt).
- [ ] **⭐ "Neurons brain" — living impact/connection map (idea 2026-06-30)** — a maintained artifact (e.g. `context/impact-map.md`) that records how the product is wired so a change in one section/flow surfaces what else to verify. No off-the-shelf tool fits (single inline-JS `index.html` + Python backend → module-graph tools see nothing); the real coupling is **shared config keys, shared DB columns, and the same logic implemented twice (JS live path vs. Python batch path)**. Layers: (1) section→section data flow (dispatch writes→planner renders→tech view shows→reports aggregate); (2) **⭐ dual-engine parity table** — pairs that MUST agree or things break: `normalizeCity()`↔`_CITY_ALIASES`, `isTenantWorkDay`↔`tenant_works_day`, `resolveRouteStrategy`↔`resolve_route_strategy`, live `_candidatesZone`↔batch `_assignment_score`; (3) config key→consumers; (4) DB column→readers/writers; (5) "if you change X → verify Y" checklist. **Motivating proof: the Sec. 5B placement bug IS a parity failure the brain would have flagged.** Build notes: derive from actual code (not memory); pair with a settings.json **hook** firing on edits to `index.html`/`backend/` so the map can't rot. Brainstorm structure before building.
- [x] **⭐ Address-level geocoding — shared "City cords" KB** ✅ 2026-07-05 (Geo Slice B): `geo_addresses` global table (deny-all RLS) + cache-first `/geocode` (exact → nearest-house-on-street → Google, IL-bbox trust, metered only on real calls). Original ask (2026-07-01): City-center coords (now 100% for PureWater's 405 zone cities in `geo_places`) are a **setup fallback, not the routing target**. Next precision step: geocode each real **street address** once (2-source-validated, reusing the 2026-06-30 Google+OSM pipeline) → store `lat/lon` **on the job** AND in a shared, cross-tenant **address cache** (city → many street addresses → coords) so repeat addresses cost ~nothing. Purpose: true door-to-door optimization (prev stop → exact address → next stop); critical when several jobs share one city (city centroid can't sequence them). **No engine change** — optimizer already prefers explicit task coords over the city brain; just populate them + add the cache table + geocode-on-save hook. Cost ~$5/1k addresses, near-zero ongoing with caching. Effort ~½ day. Extends the shared geo brain (Layer A).
- [ ] **⭐ Supabase Free → Pro (~$25/mo) — at PureWater go-live.** Driver is data safety + reliability: Free **pauses after 7d inactivity** and has **no auto-backups** (we've had data-loss incidents). The first real foundational spend. Full plan: `outputs/infra-cost-roadmap_2026-06-14.md`.
- _Client-driven costs (storage/photos, SMTP, Twilio/WhatsApp, domain, higher Maps budget) = "on the way", added per client need and covered by their price — see the infra-cost-roadmap doc._

## 🟡 After Client #2
- [ ] Custom domain (maslul.co.il) + Cloudflare
- [ ] Client #2 onboarding — `context/clients/[name].md` + SQL script
- [ ] SMS auto-send (Twilio, ~$5/mo for 100 msgs)

## 🔵 Future
- [ ] AI call summary in tech view
- [ ] Customer self-booking portal
- [ ] Native mobile app (PWA first)
- [ ] WAL tenant isolation on replay

---

## Milestone Log
| Date | What shipped |
|---|---|
| 2026-05-27 | GPS tracking, photo upload, job history, polygon zone drawing, digital signature |
| 2026-05-28 | WhatsApp template, tenant separation (Eran ↔ Israel impersonation) |
| 2026-06-01 | Configurable scheduling engine (modes, route_strategy, duration overrides), recurring jobs, pending queue, cities autocomplete, Maps quota, break time, code review |
| 2026-06-04 | UI/UX overhaul — SVG sidebar, KPI cards, tech cards, two-column dispatch |
| 2026-06-06 | PureWater zone setup — 9 zones, 3-tech rotation, city normalization |
| 2026-06-07 | Dispatch UX, task edit modal, 108 tasks seeded, polygon fix, 255 cities, geo-intelligence layer |
| 2026-06-08 | Service windows (DB + dispatch), 72/48/24h slot release (PureWater config), backtrack detection, return_city OR-Tools end depot, calendar rebuilt (absolute grid, one-tech, all tasks visible), auth-users + zones-polygons context files, 108 real tasks seeded |
| 2026-06-08 (cont.) | Batch scheduler (`/batch-schedule` + `batch_schedule.py`) — 108 PureWater tasks auto-assigned from Jun 7 with zone rotation verified (16 tech-day combos ✅); weekly calendar shows all tasks (no +N truncation); professional block styling (accent border + shadow) |
| 2026-06-09/10 | **Zones & Polygons foundation** (branch `zones-polygons`) — two-axis model (`scheduling.mode` × `zone_match` city_list/polygon) via `resolveZone()` seam; `canonicalCity` duplicate-spelling guard; Leaflet self-hosted in `vendor/` (fixes recurring map-load failure) + lazy fallback; zone authoring (canonical guard + larger draw map + `polygons[]`); per-tech `blocked_zones`; mode-aware no-match block + fix-it CTA; bulk task import; dependency-free Node test harness (`tests/zones.test.js`, 18 tests) + `/test-zones` command; `context/clients/` profile layer + doc-sync discipline. Migration applied. ⏳ needs browser QA before merge to main |
| 2026-06-13 | **Editable calendar + geo foundation (Opus session)** — daily within-grid drag (drop a call onto a 3-hour window band, `windowAtOffset` snap + dashed indicator, re-sequences); weekly cells sort window-first (fixes dragged-call float-to-top); lock/unlock pin toggle (`toggleTaskLock`) in task-detail modal. Geo brain wired into live resolution (`geo_resolver`, fail-safe) — optimizer/batch resolve through `geo_places`+`place_aliases` then `cities.py`. PureWater `auto_sequence` + `balance` turned **ON** (live-verified). Geo-corrections-loop design doc (later phase, routing untouched). 61/61 JS + 64/64 backend. Outputs folder tidied (`outputs/archive/`). ⏳ later QA: drop re-sequence round-trip, cross-tech drag, out-of-zone drop safeguard. |
| 2026-06-09→13 | **Scheduling engine B1→B3 (Fable session)** — B1 drive-time cache (global `route_cache`, cache-first matrix, physics trust-bounds, honest quota → `gmaps-cached` mode; **verified live: 0-quota cache hit**); B2 authoritative auto-sequencing (`markDayDirty`→debounced→epoch-guarded `sequenceDay`→awaited persists, flag-gated `features.auto_sequence`); B3 route-strategy physics (cost/time-callback split so far→near *emerges*), weekly balance, gap-fill on cancel, shadow-compare modal (PureWater go/no-go gate), optimistic versioning. Plus a fresh-eyes product audit that fixed a 🔴 WAL cross-tenant write bug, unmetered `/geocode`, and per-row `auth.uid()` RLS. All flag-gated → PureWater untouched. 45/45 backend + 41 JS tests. Retro + lessons → `outputs/fable-session-retro_2026-06-13.md` |
