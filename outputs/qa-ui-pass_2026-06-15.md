# QA Pass — Latest UI Changes (2026-06-15)

> Manual E2E checklist for the just-shipped UI work: the 7 ported `md-*` redesign slices
> + side-panel impersonation-chip removal + bulk-import→batch + קריאות action-row declutter.
> Source of the change set: `context/design-system.md` Change Log + `mockups/DESIGN-LOG.md`.
> Run in **incognito** at https://eranzivo.github.io/Maslul/ (use `?clearall=1` if state is stale).
> Log in as Eran → 🔀 impersonate PureWater for the multi-tenant/zone paths.
>
> **Pass rule (UI-testing rule):** on each affected screen, click *every* button/nav/card,
> confirm it navigates or triggers, verify the engine path still fires, and confirm **no console errors**.
> Mark each `[ ]` → `[x]`; log any fix found in the "Fixes found" table at the bottom.

## ✅ Static QA pass (automated, 2026-06-15) — no browser needed
Ran across the whole shipped redesign (all `md-*` slices + bulk-import + calls declutter):
1. **Inline JS parse-check** (`vm.Script` over both inline `<script>` blocks): **0 syntax errors**. The port introduced no parse breaks.
2. **Handler resolution** (every `on*=` attribute → called fn must be defined): **182 handler calls, all resolve.** The 10 "unresolved" are false positives — DOM/array methods (`classList.add/remove/toggle`, `.closest`, `.find`, `.stopPropagation`), `getElementById`, `window.print`, CSS `rgba(`/`var(` inside inline styles. **No broken button wiring.**
3. **ID cross-check** (`getElementById`/`querySelector('#…')` vs all static+generated `id=`): **308 referenced, only 1 absent — `role-chips`** — and both references are **null-safe** (`if(!container)return;` @ index.html:3419, `?.classList` @ :3562). It's the removed impersonation-chips element (relocation to Technicians page pending). **Not a bug; dead-code cleanup only** (`buildRoleSelect()` + `.role-chip*` CSS).

**Verdict:** the redesign port is structurally sound — no syntax errors, no dangling handlers, no broken ID refs. Remaining QA = **visual/interaction** items below, which need a browser (Eran's "QA on the way").

---

## 0 — Smoke / load (do first)
- [ ] Login < 3s, succeeds, home renders (no white screen, no console errors).
- [ ] Sidebar shows brand mark + nav; active item has the indigo edge bar (Slice 7).
- [ ] Switch Eran → PureWater via 🔀 chip; tenant card (`renderSidebarTenant`) updates to PureWater.
- [ ] Hard-refresh on each main page (`#page-home`, dispatch, planner, tasks) — no page lands blank.

## 1 — Sidebar + tenant card (Slice 7, commit `f7b7b78`)
- [ ] Every nav item navigates to its page (לוח בקרה/home, שיבוץ/dispatch, יומן/planner, קריאות/tasks, טכנאים, אזורים, דוחות, לקוחות, הגדרות).
- [ ] Active page = indigo fill + bold + 3px edge bar; only one active at a time.
- [ ] `.md-tenant-card` shows correct company + role; logout row works.
- [ ] Zone nav (אזורים) **hidden** for `open`/`radius` tenants — N/A for PureWater (zone mode) but confirm it's *visible* here.
- [ ] Mobile width: `#mob-bar` bottom nav still works, no overlap with the desktop sidebar.

## 2 — Side panel: impersonation chips removed (commit `c154f91`)
- [ ] Technician impersonation chips are **gone** from the side panel (relocated to Technicians page in Phase 2).
- [ ] No dead space / broken layout where the chips used to be.
- [ ] Eran's tenant 🔀 switch still works (that's separate from the removed tech chips).

## 3 — Home dashboard (Slice 6, commit `bb8c7d4`)
- [ ] KPI strip renders 4 cards with top-border accents: ממתין לשיבוץ (amber), משובץ (indigo), הושלם (green), היום (slate).
- [ ] KPI **numbers match reality** — cross-check ממתין count vs the קריאות "ממתינות" filter count.
- [ ] "צוות בשטח" tech grid renders one `tech-home-card` per technician (3 for PureWater); colors match `technician.color`.
- [ ] Click a tech card → weekly slide-in opens with that tech's week (days · windows · cities).
- [ ] Slide-in "פתיחת מסלול יומי" → opens the daily grid for that tech.
- [ ] Header buttons (רענון, שיבוץ קריאה) both fire; שיבוץ קריאה lands on the search/dispatch flow.
- [ ] Renders cleanly at 1, 2, 3+ techs (impersonate / mental-check the grid won't break with 5).

## 4 — Coordinator 3-card chooser (Slice 5, commit `16b51ba`, `showCandidateCards` @ index.html:5739)
- [ ] Search (city + address + service type) → "חיפוש מועדים" produces **3 recommendation cards**, best-first.
- [ ] Card content = weekday + date + **time-window chip only** — NO tech / route / score shown on the card.
- [ ] Card 1 = engine best: indigo border + glow + "מומלץ ★" badge + filled button.
- [ ] **RTL time window reads start→end** ("07:00–10:00", NOT "10:00–07:00") — the known port risk. Check the chip on all 3 cards.
- [ ] "בחירה" on a card → reveal step shows tech + day route (reuses `showCandidate` route block) → "אישור שיבוץ ✓" persists.
- [ ] **Engine wiring:** confirmed assignment writes to `tasks` (window_start/end + estimated time), appears on the calendar, survives refresh. (Dry-run mindset: this is a live write — verify it's the test call you intend.)
- [ ] "מצא מועד אחר" → next-best slice of candidates (cards refresh, still rule-compliant).
- [ ] Fallback intact: old `showCandidate` path (index.html:5860) still reachable / not broken by the additive cards.
- [ ] Card ordering is chronological / best-first — never a later date above an earlier one with worse score out of order.

## 5 — Weekly board: 3h window blocks (Slice 4, commit `e785ec8`, `_plannerWeekCell` @ index.html:7482)
- [ ] Weekly view renders window blocks (count badge + time range + stacked client·city rows + status dot).
- [ ] **Multiple jobs stack inside one 3h window** (e.g. several in 07:00–10:00) — the PureWater model.
- [ ] Empty cell shows "פנוי"; legend correct (משובץ indigo / ממתין amber / הושלם green).
- [ ] **Chronological sort within a cell** — 07:00 window above 10:00, never reversed (the #1.8 audit point).
- [ ] Calendar shows **only confirmed/completed**, not drafts/candidates.
- [ ] Window time labels are LTR + tnum (read start→end).
- [ ] Renders at 1 / 2 / 3+ techs and narrow widths without layout break.
- [ ] Drag a chip between tech/day cells → `reassignTask` fires, persists, both days re-flow.

## 6 — Daily route grid (Slice 3, commit `25214e3`, `renderPlannerDaily` @ index.html:7575)
- [ ] **Geometry unchanged** (the hard rule): hour labels align, blocks sit at correct times (PX=1, 60px/hour).
- [ ] 3-hour window blocks positioned by time; overlapping windows sit **side-by-side in columns** (`layoutColumns`), nothing covers anything.
- [ ] Red **now-line** renders at current time with "● HH:MM".
- [ ] Drive-time hints ("N דק׳ מ-origin") + status dots render inside job rows.
- [ ] "ממתין לשיבוץ N" tray below the grid; rows are draggable (handle + client + chips).
- [ ] **Within-grid drag:** drag a tray row onto a window band → dashed snap indicator → drops → `windowAtOffset` snaps → assigns/persists/re-sequences.
- [ ] Out-of-zone drop → `confirmZoneDrop` warns (fail-soft) when city's zone ≠ tech's day zone; ⚠ shows on the call.
- [ ] "מסלול מיטבי" (optimize) button → `runOptimize`/`_postOptimize` fires; locked tasks stay pinned, windows preserved.
- [ ] Tech tabs at top switch between techs; utilization ring (N / N קריאות) correct.

## 7 — Task detail side-panel (Slice 2, commit `db2a49d`, `#mo-task-detail`, `openTaskDetail` @ index.html:7731)
- [ ] Click any calendar block / task row → left-anchored panel slides in (translateX + scrim).
- [ ] Header: eyebrow "קריאה #id" + close; client name + status pill.
- [ ] Body rows: מיקום, חלון הגעה (`direction:ltr`), טכנאי (avatar+name+region), סוג שירות chip, הערות — no repeated/missing data.
- [ ] Footer: עריכה (primary) opens edit; **lock toggle** (`toggleTaskLock` @ index.html:7796) flips 🔒/🔓, persists, re-sequences the day; ביטול (danger) cancels.
- [ ] שיבוץ/העברה panel (`placeTaskDetail` @ index.html:7825): tech select + date + 3h-window select → places/moves the call, flips pending→assigned, persists, marks both days dirty.
- [ ] Esc **and** scrim click both close the panel.
- [ ] Re-open a different task — panel repopulates correctly (no stale data from the previous task).

## 8 — קריאות action-row declutter (commit `40629f9`, `tf-*` ids)
- [ ] Header actions present: **+ הוסף קריאה** (primary), ייבא CSV + ייבא מרובה (secondary) — visually separated from filters.
- [ ] Filter wrap-bar with divider holds the 7 filters; each toggles its list:
  - [ ] `tf-all` הכל · `tf-pending` ממתינות · `tf-assigned` משובצות · `tf-completed` הושלמו · `tf-cancelled` בוטלו · `tf-recurring_unassigned` ⚠️ חוזרות ממתינות · `tf-recurring_templates` 🔁 תבניות.
- [ ] Active filter is visually highlighted; counts plausible vs home KPIs.
- [ ] No filter row wrap/overflow ugliness at narrow widths.

## 9 — Bulk-import → batch engine (commit `5acf4cd`, `runBulkImport` @ index.html:6333, `#mo-bulk-import`)
> ⚠️ This path can write live data. **Always run the dry-run PREVIEW first; only commit a deliberate test set.**
- [ ] "ייבא מרובה" opens `#mo-bulk-import`; paste `street, city` rows.
- [ ] **⚡ שבץ אוטומטית** → calls `/batch-schedule` with the user's own Supabase JWT (tenant forced to caller's).
- [ ] **Dry-run PREVIEW** renders before any write: shows planned assignments + per-tech-day, and unmatched/flagged rows (`needs_location`, `city_not_in_zone`).
- [ ] **Commit** only after preview review → tasks persist, appear on calendar, survive refresh.
- [ ] Unmatched rows list reasons + "תקן אזורים" CTA navigates to zones page.
- [ ] Zone rotation respected (a call only lands on the tech covering that zone that weekday).
- [ ] **No Fri/Sat assignments** (PureWater Sun–Thu) — verify nothing landed on Friday/Saturday.
- [ ] Auth negative check (mental/spot): a tech-role user is denied batch; super_admin impersonation is honored.

## 10 — Cross-cutting regression sweep
- [ ] No `md-*` class clobbered an existing generic class (`.btn .card .nav .page .pill .status-pill .kpi`) — buttons/cards across *old* pages (reports, clients, technicians, zones, settings, categories) still styled correctly.
- [ ] Reports page renders + any export/filter buttons fire.
- [ ] Technicians page loads; edit drawer opens (rotation grid for zone mode; Leaflet uses `LF`, no "L is not a function").
- [ ] Zones page loads; polygon draw map loads (vendored Leaflet, not CDN).
- [ ] Settings page loads; toggles (`tf-crm`, `tf-files`, `tf-checklists`, `tf-reports`, `tf-whatsapp`) flip + persist.
- [ ] Clients page loads + add/edit works.
- [ ] Role/tenant switch round-trip: Eran → PureWater → back to Eran, no stale tenant data.
- [ ] Tech-view (login as a tech, or impersonate) shows only that tech's schedule.
- [ ] **Console clean across the whole sweep** (no red errors, no failed fetches).

---

## Fixes found (log each as you go)
| # | Screen / area | Symptom | Severity | Fix / commit |
|---|---|---|---|---|
| 1 | Zones — polygon draw (`_initZoneDrawMap` @ index.html:8266) | Existing zone polygon can't be adjusted — re-opening only shows city dots, not the saved `z.polygons[0]` as an editable layer, so you must redraw. Vertex-edit not surfaced (must find leaflet-draw pencil). | Med | Load `z.polygons[0]` into `_zoneDrawFeatureGroup` + auto-enable edit handles; sync `_drawnPolygon` on `editvertex`. *(pending)* |
| 2 | Zones — polygon draw modal (`#mo-zone-draw` @ index.html:1954) | "Can't go back" while drawing: ביטול button is below the 70vh map (off-screen); visible leaflet-draw toolbar is English (Cancel/Delete last point/Finish) and only cancels the draw, not the screen. | Med-High (user got stuck) | Persistent ✕ close in modal title row + localize leaflet-draw toolbar to Hebrew via `LF.drawLocal`. *(pending)* |

---

## Engine / scheduling correctness findings (separate from UI — needs live-data verification)
> PureWater live calendar 7.6–12.6.2026 (אלירן row). Eran flagged route quality 2026-06-15.

| # | Issue | Diagnosis | Status / next |
|---|---|---|---|
| E1 | אלירן Sun packed with ~5 באר שבע, מיכאל ~1 | **Config-correct, not a bug** — `balance.enabled = OFF` (set 2026-06-14, feedback #1.5) = fill-first consolidation packs one tech to max before opening another. ⚠ If a באר שבע call sits on מיכאל **Thursday** (ירושלים zone), that IS a hard zone violation — verify. | Decision: keep consolidation or flip `balance.enabled = ON` (spread across covering days). Config flip, not code. |
| E2 | 20+ pending; "should fit the week" | **Zone-coverage mismatch, known/documented.** Capacity fits (135 slots / 108 calls). תל אביב has ~27 calls but only 2 covering tech-days (בני Sun + אלירן Wed = max 18) → ~9 can't place. Logged 2026-06-14. | Backlog **#2.2** — give busy zones a 3rd covering tech-day (rotation redesign). |
| E4 | **Duplicate/orphan zones (data integrity)** | PureWater has **15 zones, only 9 in the tech rotation**. 6 orphans — גוש דן, גליל, חיפה וקריות, שפלה, שרון (+ empty `אזור חדש`) — **overlap the same cities** as rotated zones (e.g. תל אביב ∈ both "תל אביב והסביבה" and "גוש דן"). `find_zone()` matches the FIRST zone containing a city; if it's an orphan no tech covers → task stranded (`no_slot_in_range`). Violates one-city→one-zone. Verified via live SQL 2026-06-15. | **Decide & clean up**: are the orphans dead test data (delete) or a newer intended set (re-point rotation + retire old)? Then dedupe so each city maps to exactly one rotated zone. Re-run batch after. ⚠ live zone edits — confirm with Eran first; not reversible without SQL. |
| E5 | 20 pending tasks — "why unassigned?" | **NOT structural — all 20 CAN be assigned.** Every pending city is in a rotated zone. Causes: (a) ~17 = capacity overflow in 2 under-covered zones in the 1-week batch (לוד-אשדוד 25 vs 18 slots, תל אביב 27 vs 18; backlog #2.2 / wider window); (b) 1 = needs-location (חרב, no coords); (c) a few = the E4 duplicate-zone misroute. | Exact per-task reason codes come from a **dry-run `/batch-schedule`** (safe, no writes) — run after the E3 fix deploys + E4 cleanup. |
| E3 | אלירן Tue 9/6 backtrack | ✅ **FIXED (engine, 2026-06-15).** `solve_route_v2` now enforces `far_to_near` direction via a dominant outward-arc penalty (fail-open; same-city adjacent). Real day now routes נהריה→קרית ים→קרית ים→קרית חיים→חיפה (non-increasing dist-from-base). 76/76 backend tests. ⏳ deploy to Railway + dry-run re-batch to refresh the live calendar (old 88 tasks not auto-corrected). **CONFIRMED real bug (verified via live `scheduled_time`).** Actual order: חיפה 10:44 → קרית ים 11:30 → נהריה 12:29 → **קרית ים 13:28** → קרית חיים 14:01. Two problems: (1) קרית ים visited TWICE with נהריה between — same-city jobs not collapsed; (2) far→near REVERSED — day starts at nearest (חיפה), ends at קרית חיים, climbs out to נהריה mid-day (should end near base). Data = 2026-06-14 batch run. | **Investigate backend batch sequencer** (`batch_schedule.py optimize_day` → `solve_route_v2`). Suspects in order: (a) same-city jobs not collapsed; (b) `far_to_near` cost/time split not ending near base in batch path; (c) missing/bad coord for one of the 4 cities. Then re-batch (dry-run) to confirm fix. |

> After completing the pass: summarize results, then for each fix decide inline-restyle vs
> `systematic-debugging`, apply per the design-system hard rules, and append a row to the
> `context/design-system.md` Change Log. UI is git-reversible; **do not** touch live DB data
> without a dry-run/preview.
